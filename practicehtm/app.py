from flask import Flask, render_template, request

app = Flask(__name__)

@app.route("/")
def index():
    if text in input:
         text = input.text
    else:
         name = "Nobody"

    return render_template("index.html", <p> "Hello" name <p>)

