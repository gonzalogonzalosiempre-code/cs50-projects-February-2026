#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int N = 0;
char TableHash[];

int main(void)
{
 char Texto[];
 printf("Ingresa Operacion: ");
 if ((fgets(Texto, sizeof(Texto), stdin)) != NULL)
 {
    strtok(Texto, " ")
 }
}

char HashClave(int arr[])
{

    arr[]
}
