#include <stdio.h>
#include <cs50.h>
#include <ctype.h>
#include <string.h>

int main(void)
{
    string KeyCifrado = get_string("Cifrado Key..");

    for (int i = 0; i < strlen(KeyCifrado); ++i)
    {
       char Letra[i] += KeyCifrado[i];
       
    }
}
